# Brewfile — DEFAULT / PERSONAL profile (David Guardia)
# Daily-driver Mac. For the lean Crabby Guard host profile, see Brewfile.mini.
# Updated 2026-05-29: dropped extra browsers, Microsoft/Adobe/consumer apps,
# R stack, legacy web tooling (grunt/gatsby/heroku/sass/hugo/dart/flutter).

# ── Taps ────────────────────────────────────────────────────────────────────
tap "buo/cask-upgrade"
tap "homebrew-ffmpeg/ffmpeg"
tap "pantheon-systems/external"
tap "teamookla/speedtest"

# ── CLI essentials ─────────────────────────────────────────────────────────
brew "ack"                     # search like grep, made for programmers
brew "brew-cask-completion"
brew "buf"                     # protobuf tooling
brew "gh"                      # GitHub CLI
brew "jq"
brew "openssh"
brew "pandoc"
brew "pipx"                    # isolated Python tools
brew "stern"                   # multi-pod log tail (k8s)
brew "subversion"              # legacy SVN access
brew "tree"
brew "wget"

# ── Languages & build ───────────────────────────────────────────────────────
brew "autoconf"
brew "fnm"                     # Node version manager
brew "node"
brew "php"
brew "composer"
brew "yarn"

# ── DevOps / cloud / WP ─────────────────────────────────────────────────────
brew "helm"
brew "pantheon-systems/external/terminus"   # Pantheon CLI (Press Ganey work)
brew "teamookla/speedtest/speedtest"

# ── Media ───────────────────────────────────────────────────────────────────
brew "imagemagick"
brew "homebrew-ffmpeg/ffmpeg/ffmpeg"

# ── Local LLM ───────────────────────────────────────────────────────────────
brew "ollama"
brew "llama.cpp"

# ── Casks: browsers (just two) ──────────────────────────────────────────────
cask "google-chrome"
cask "firefox"

# ── Casks: dev tools ────────────────────────────────────────────────────────
cask "visual-studio-code"
cask "zed"
cask "phpstorm"
cask "cyberduck"
cask "postman"
cask "figma"
cask "ollama-app"

# ── Casks: cloud SDKs ───────────────────────────────────────────────────────
cask "google-cloud-sdk"

# ── Casks: communication & media ────────────────────────────────────────────
cask "slack"
cask "vlc"

# ── VS Code extensions ──────────────────────────────────────────────────────
vscode "anthropic.claude-code"
vscode "davidanson.vscode-markdownlint"
vscode "github.copilot"
vscode "github.copilot-chat"
vscode "ms-azuretools.vscode-containers"
vscode "ms-playwright.playwright"
vscode "ms-python.debugpy"
vscode "ms-python.python"
vscode "ms-python.vscode-pylance"
vscode "ms-python.vscode-python-envs"
vscode "ms-vscode-remote.remote-containers"
vscode "ms-vscode-remote.remote-ssh"
vscode "openai.chatgpt"
vscode "shd101wyy.markdown-preview-enhanced"
